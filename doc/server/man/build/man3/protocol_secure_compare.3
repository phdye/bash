'\" t
.\" Automatically generated by Pandoc 3.8.3
.\"
.TH "protocol_secure_compare" "3" "2026\-02\-01" "bash\-server 0.1.0" "bash\-server library"
.SH SYNOPSIS
.IP
.EX
#include \(dqserver.h\(dq

int protocol_secure_compare(const char *a, const char *b);
.EE
.SH DESCRIPTION
Performs a constant\-time comparison of two NUL\-terminated strings to
prevent timing side\-channel attacks when verifying authentication
tokens.
.PP
If the strings differ in length, the function returns 0 immediately.
This reveals the length mismatch but is acceptable for fixed\-length
hex\-encoded tokens (which are always \f[CR]SERVER_TOKEN_HEXLEN\f[R] =
64 characters).
.PP
When the strings have equal length, every byte is compared using XOR
accumulation.
The comparison always processes the full length regardless of where the
first difference occurs, ensuring that the execution time does not leak
information about the position of mismatching characters.
.PP
This function is used by the authentication handler to verify client
tokens against the server\(cqs stored token.
.SH PARAMETERS
.PP
.TS
tab(@);
l l l.
T{
Parameter
T}@T{
Type
T}@T{
Description
T}
_
T{
\f[CR]a\f[R]
T}@T{
\f[CR]const char *\f[R]
T}@T{
First NUL\-terminated string.
T}
T{
\f[CR]b\f[R]
T}@T{
\f[CR]const char *\f[R]
T}@T{
Second NUL\-terminated string.
T}
.TE
.SH RETURN VALUE
Returns \f[B]1\f[R] if the strings are identical (same length and same
content).
.PP
Returns \f[B]0\f[R] if the strings differ in length or content.
.SH ERRORS
No error conditions.
The function always returns 0 or 1.
.SH EXAMPLES
.IP
.EX
\f[I]/* Verify client authentication token */\f[R]
\f[B]if\f[R] (protocol_secure_compare(client_token, config\->auth_token)) {
    session\->authenticated = 1;
    protocol_write_line(fd, \(dqOK authenticated\(dq);
} \f[B]else\f[R] {
    protocol_write_line(fd, \(dqERR invalid token\(dq);
}
.EE
.SH SEE ALSO
\f[CR]session_handle(3)\f[R], \f[CR]protocol_read_line(3)\f[R]
.SH NOTES
.IP \(bu 2
The comparison is constant\-time only when both strings have the same
length.
A length mismatch returns 0 immediately, which could reveal that the
lengths differ.
For the bash\-server protocol, tokens are always 64 hex characters
(\f[CR]SERVER_TOKEN_HEXLEN\f[R]), so this early return only triggers for
malformed input.
.IP \(bu 2
The XOR accumulation technique (\f[CR]result |= a[i] \(ha b[i]\f[R])
ensures that all bytes are compared even if an early byte differs.
The final check \f[CR]result == 0\f[R] is true only if every byte
matched.
.IP \(bu 2
This function is not suitable for comparing binary data with embedded
NUL bytes, as it relies on \f[CR]strlen(3)\f[R] to determine string
length.
.IP \(bu 2
The function uses \f[CR]unsigned char\f[R] casts for the XOR to avoid
sign\-extension issues.
