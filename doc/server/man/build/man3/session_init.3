.\" Automatically generated by Pandoc 3.8.3
.\"
.TH "session_init" "3" "2026\-02\-01" "bash\-server 0.1.0" "bash\-server library"
.SH SYNOPSIS
.IP
.EX
#include \(dqserver.h\(dq

int session_init(client_session_t *session, int client_fd);
.EE
.SH DESCRIPTION
Initializes a \f[CR]client_session_t\f[R] structure for a new client
connection.
Zeroes the entire structure with \f[CR]memset\f[R], then sets the
primary file descriptor to \f[CR]client_fd\f[R] and establishes default
values for all fields.
.PP
The session starts unauthenticated, using protocol version
\f[CR]PROTOCOL_V1\f[R] with \f[CR]WIRE_BINARY\f[R] wire format.
The write file descriptor is set to \f[CR]\-1\f[R], indicating that the
primary \f[CR]fd\f[R] should be used for both reading and writing (the
common case for Unix domain sockets).
Pipe file descriptors for stdout/stderr capture are initialized to
\f[CR]\-1\f[R] (closed).
.PP
The session\(cqs \f[CR]pid\f[R] field is set to the current process ID
via \f[CR]getpid()\f[R].
.PP
This function must be called before \f[CR]session_handle()\f[R] or any
other session operation.
.SH PARAMETERS
.IP \(bu 2
\f[B]session\f[R] \(en Pointer to the \f[CR]client_session_t\f[R]
structure to initialize.
Must not be NULL.
.IP \(bu 2
\f[B]client_fd\f[R] \(en File descriptor for the connected client.
For socket transport, this is the fd returned by
\f[CR]server_accept_client()\f[R].
For stdio transport, this is \f[CR]STDIN_FILENO\f[R].
For \f[CR]\-\-fd\f[R] transport, this is the inherited file descriptor
number.
.SH RETURN VALUE
Returns \f[CR]0\f[R] on success.
Currently always succeeds.
.SH ERRORS
None.
The function does not perform any system calls that can fail.
.SH SEE ALSO
\f[CR]session_cleanup\f[R](3), \f[CR]session_handle\f[R](3),
\f[CR]server_accept_client\f[R](3)
.SH NOTES
After \f[CR]session_init()\f[R], the caller may set
\f[CR]session\->write_fd\f[R] to a different file descriptor if the read
and write channels are separate (e.g., stdio mode where
\f[CR]fd=STDIN_FILENO\f[R] and \f[CR]write_fd=STDOUT_FILENO\f[R]).
.PP
The \f[CR]client_session_t\f[R] structure is defined in
\f[CR]server.h\f[R]:
.IP
.EX
typedef struct client_session {
    int   fd;              /* Primary fd (read, or read+write) */
    int   write_fd;        /* Write fd (\-1 = use fd for both) */
    int   authenticated;   /* 1 after successful AUTH */
    int   protocol_version;/* PROTOCOL_V1, V2, or V2_NDJSON */
    int   wire_format;     /* WIRE_BINARY or WIRE_NDJSON */
    pid_t pid;
    int   stdout_pipe[2];
    int   stderr_pipe[2];
} client_session_t;
.EE
