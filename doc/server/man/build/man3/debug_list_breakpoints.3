'\" t
.\" Automatically generated by Pandoc 3.8.3
.\"
.TH "debug_list_breakpoints" "3" "2026\-02\-01" "bash\-server 0.1.0" "bash\-server library"
.SH SYNOPSIS
.IP
.EX
#include \(dqserver.h\(dq

char *debug_list_breakpoints(void);
.EE
.SH DESCRIPTION
Serializes all breakpoints in the debugger\(cqs linked list to a JSON
array string.
Each breakpoint is represented as a JSON object with the following
fields:
.PP
.TS
tab(@);
l l l.
T{
Field
T}@T{
Type
T}@T{
Description
T}
_
T{
\f[CR]id\f[R]
T}@T{
int
T}@T{
Breakpoint ID
T}
T{
\f[CR]type\f[R]
T}@T{
string
T}@T{
\f[CR]\(dqcommand\(dq\f[R], \f[CR]\(dqline\(dq\f[R], or
\f[CR]\(dqfunction\(dq\f[R]
T}
T{
\f[CR]enabled\f[R]
T}@T{
bool
T}@T{
Whether the breakpoint is active
T}
T{
\f[CR]hit_count\f[R]
T}@T{
int
T}@T{
Number of times this breakpoint has been hit
T}
T{
\f[CR]pattern\f[R]
T}@T{
string
T}@T{
Match pattern (present if non\-NULL)
T}
T{
\f[CR]line\f[R]
T}@T{
int
T}@T{
Line number (present for LINE breakpoints)
T}
T{
\f[CR]condition\f[R]
T}@T{
string
T}@T{
Condition expression (present if non\-NULL)
T}
.TE
.PP
\f[B]Example output:\f[R]
.IP
.EX
[{\(dqid\(dq:1,\(dqtype\(dq:\(dqcommand\(dq,\(dqenabled\(dq:true,\(dqhit_count\(dq:3,\(dqpattern\(dq:\(dqecho\(dq},
 {\(dqid\(dq:2,\(dqtype\(dq:\(dqline\(dq,\(dqenabled\(dq:true,\(dqhit_count\(dq:0,\(dqline\(dq:42}]
.EE
.SH PARAMETERS
None.
.SH RETURN VALUE
Returns a \f[CR]malloc()\f[R]\(cqd string containing the JSON array.
The caller must \f[CR]free()\f[R] the returned string.
.PP
Returns an empty array \f[CR]\(dq[]\(dq\f[R] if no breakpoints exist.
.PP
The internal buffer is approximately 4 KB; breakpoint lists that exceed
this size are truncated.
.SH SEE ALSO
\f[CR]debug_add_breakpoint\f[R](3),
\f[CR]debug_remove_breakpoint\f[R](3),
\f[CR]debug_handle_message\f[R](3)
.SH NOTES
.IP \(bu 2
The serialization uses \f[CR]snprintf()\f[R] into a fixed 4096\-byte
stack buffer, then \f[CR]strdup()\f[R]\(cqs the result.
Very long patterns or conditions may cause truncation.
.IP \(bu 2
Breakpoints are listed in reverse insertion order (most recently added
first), because new breakpoints are prepended to the linked list.
.IP \(bu 2
Pattern strings are not JSON\-escaped in the current implementation.
Patterns containing double quotes or backslashes may produce malformed
JSON.
