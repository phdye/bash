'\" t
.\" Automatically generated by Pandoc 3.8.3
.\"
.TH "json_set_wire_format" "3" "2026\-02\-01" "bash\-server 0.1.0" "bash\-server library"
.SH SYNOPSIS
.IP
.EX
#include \(dqserver.h\(dq

void json_set_wire_format(int format);
.EE
.SH DESCRIPTION
Sets the wire format used by \f[CR]json_frame_read(3)\f[R] and
\f[CR]json_frame_write(3)\f[R] for the current process.
The format determines how v2 protocol frames are serialized on the wire.
.PP
.TS
tab(@);
l l l.
T{
Constant
T}@T{
Value
T}@T{
Description
T}
_
T{
\f[CR]WIRE_BINARY\f[R]
T}@T{
0
T}@T{
6\-byte binary header + payload
T}
T{
\f[CR]WIRE_NDJSON\f[R]
T}@T{
1
T}@T{
Newline\-delimited JSON with \f[CR]\(dqch\(dq\f[R] field
T}
.TE
.PP
This function should be called once per session, immediately after
protocol version detection (see \f[CR]protocol_detect_version(3)\f[R])
and before any v2 frame I/O.
.PP
The setting is stored in a process\-global static variable.
This is safe because bash\-server uses a fork\-per\-session model: each
client session runs in its own process.
.SH PARAMETERS
.PP
.TS
tab(@);
lw(10.4n) lw(6.6n) lw(53.0n).
T{
Parameter
T}@T{
Type
T}@T{
Description
T}
_
T{
\f[CR]format\f[R]
T}@T{
\f[CR]int\f[R]
T}@T{
Wire format: \f[CR]WIRE_BINARY\f[R] (0) or \f[CR]WIRE_NDJSON\f[R] (1).
T}
.TE
.SH RETURN VALUE
None (\f[CR]void\f[R]).
.SH ERRORS
No error checking is performed.
Passing an invalid format value leads to undefined behavior in
subsequent frame I/O calls.
.SH EXAMPLES
.IP
.EX
char first_byte;
int version = protocol_detect_version(client_fd, &first_byte);

\f[B]if\f[R] (version == PROTOCOL_V2) {
    json_set_wire_format(WIRE_BINARY);
    json_session_handle(session, config);
} \f[B]else\f[R] \f[B]if\f[R] (version == PROTOCOL_V2_NDJSON) {
    json_set_wire_format(WIRE_NDJSON);
    json_session_handle(session, config);
}
.EE
.SH SEE ALSO
\f[CR]json_get_wire_format(3)\f[R], \f[CR]json_frame_read(3)\f[R],
\f[CR]json_frame_write(3)\f[R], \f[CR]protocol_detect_version(3)\f[R],
\f[CR]json_session_handle(3)\f[R]
.SH NOTES
.IP \(bu 2
The default wire format is \f[CR]WIRE_BINARY\f[R] (0).
.IP \(bu 2
This function is not thread\-safe, but thread safety is not required
because each session runs in a separate forked process.
.IP \(bu 2
Changing the wire format mid\-session is not supported and will corrupt
the protocol stream.
.IP \(bu 2
The wire format affects both reading and writing:
\f[CR]json_frame_read(3)\f[R] and \f[CR]json_frame_write(3)\f[R] both
check the global setting on every call.
